
<resources
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">

        <!-- Color theme -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Drawer arrow -->
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

    </style>

    <!-- NoActionBar with DarkActionBar theme. -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- *********************************** Toolbar ******************************************* -->

    <!-- Drawer arrow style -->
    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <!-- Toolbar style -->
    <style name="ToolbarStyle" parent="ThemeOverlay.AppCompat.Dark">
        <item name="titleTextColor">@android:color/white</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:elevation" tools:targetApi="21">@dimen/elevation_toolbar</item>
    </style>

    <!-- *********************************** NavigationView ************************************ -->

    <!-- NavigationView style -->
    <style name="NavigationViewStyle">
        <item name="headerLayout">@layout/header_drawer_activity_main</item>
        <item name="menu">@menu/activity_main_menu_drawer</item>
        <item name="itemIconTint">@android:color/white</item>
        <item name="itemTextColor">@android:color/white</item>
        <item name="android:background">@color/colorPrimaryLight</item>
    </style>

    <!-- *********************************** TextView ****************************************** -->

    <!-- TextView style (level 1, black, normal) -->
    <style name="TextViewStyle1" parent="@style/TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">15sp</item>
    </style>

    <!-- TextView style (level 1, darker gray, normal) -->
    <style name="TextViewStyle2" parent="@style/TextAppearance.MaterialComponents.Body2">
        <item name="android:textColor">@android:color/darker_gray</item>
    </style>

    <!-- TextView style (level 1, darker gray, normal) -->
    <style name="TextViewStyle3" parent="@style/TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <!-- TextView style (level 1, white, normal) -->
    <style name="TextViewStyle4" parent="@style/TextAppearance.MaterialComponents.Body2">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- *********************************** Button ******************************************** -->

    <!-- Borderless Button style -->
    <style name="BorderlessButtonStyle" parent="@style/Widget.MaterialComponents.Button.TextButton">
    </style>

    <!-- Button style -->
    <style name="ButtonStyle" parent="@style/Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/colorAccent</item>
    </style>

    <!-- *********************************** FloatingActionButton ****************************** -->

    <!-- FloatingActionButton style -->
    <style name="FloatingActionButtonStyle" parent="@style/Widget.MaterialComponents.FloatingActionButton">
        <item name="backgroundTint">@color/colorAccent</item>
        <item name="tint">@android:color/white</item>
    </style>

</resources>